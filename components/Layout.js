export const siteTitle = 'Friends of Stringers Ridge'

export default function Layout({children}) {
  return (
    <div>
        <nav>
        </nav>

        <div>{children}</div>

        <footer>
        </footer>
    </div>
  )
}